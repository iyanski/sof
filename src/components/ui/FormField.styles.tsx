import type { CSSProperties } from 'react'

export const styles = {
  required: {
    color: 'red',
    marginLeft: '2px'
  } as CSSProperties,
  errorMessage: {
    marginTop: '0.5rem'
  } as CSSProperties
}
